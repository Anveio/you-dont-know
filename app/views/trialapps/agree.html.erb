<% provide(:title, "About Us") %>
<h1>First, a little about us</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <h3> Placeholder Text.</h3>
  </div>
</div>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for(:user, url: password_resets_path) do |f| %>
      <%= f.label "I've read the Terms and agree." %>
      <%= f.check_box :agreed %>
      <%= f.submit "Proceed to Application", class: "btn btn-primary" %>
    <% end %>
  </div>
</div>

